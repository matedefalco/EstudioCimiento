Surface container. Default `flat` (hairline only); raise only genuinely floating layers.

```jsx
<Card elevation="raised" accentEdge>
  <h3>relevamiento</h3>
  <p>entender cómo funciona hoy el negocio.</p>
</Card>
```

`elevation`: `flat | raised | floating`. `accentEdge` adds a bronze top hairline.
