Labeled text field with hairline border and bronze focus ring.

```jsx
<Input label="tu negocio" placeholder="cómo se llama" hint="lo usamos para el relevamiento" />
<Input label="email" type="email" error="revisá el formato" />
```
