Brand lockup: ascending steps symbol + lowercase "estudio cimiento" wordmark. Logotype is always lowercase.

```jsx
<Logo size={42} />            {/* full lockup */}
<Logo variant="symbol" size={48} />  {/* mark only */}
```

Accent (steps + "estudio") auto-themes: bronze in día, copper inside `[data-theme="night"]`.
